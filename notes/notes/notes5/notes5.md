### 1.

 #### ls - list what contents are in a selected directory, none would default to the one that is current one selected in the terminal.

 1. ls /home/kaizer
 2. ls - only by itself alone to display the current directory in the terminal. 

 #### pwd - shows where in the file system you are currently on like a location on a gps.

* For example - /home/kaizer/Downloads
  
 #### cd - Change directory; meaning to change the path in of the directory.

* Example : cd /
### 2.

1. variable is like a blank slate; it can have a value that makes it useful and even create shortcuts if composed properly. 


2.  Assign a value or purpose to the variable to make it active ;for example, variable = 1 or variable = echo -e "Helloworld/n/t/tIce cubes are cold." in the bash file type


3. Log specific system and user information like $HOME. It's just built in with in the systems memory with in.


4. The user makes the variable.

5. The base like the root of the tree and like roots of a tree is important. it contains files that makes the OS.


6.  The prior directory of the current one except root.

7. The location where you are at the moment ;for instance, GPS or your right here in guide maps for Directory.

8. Use root as the starting point for a file. An example, /home/kaizer/Downloads.


9.  In the selected directory ot within a directory that houses the current directory. Pictures/Kale.png


10. The difference is that the user's home directory houses the user's content while "The home directory" houses the users.
